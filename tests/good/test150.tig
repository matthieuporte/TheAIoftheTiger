let
  type a = array of int
  var x := a[5] of 0
  var i := 0
  var sum := 0
  var avg := 0
in
  x[0] := 2;
  x[1] := 4;
  x[2] := 6;
  x[3] := 8;
  x[4] := 10;
  
  while i < 5 do (
    sum := sum + x[i];
    i := i + 1
  );
  avg := sum / 5;
  print_int(avg)
end
