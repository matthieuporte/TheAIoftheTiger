let
  type a = array of int
  var x := a[5] of 0
  var i := 0
  var sum := 0
in
  x[0] := 1;
  x[1] := 2;
  x[2] := 3;
  x[3] := 4;
  x[4] := 5;
  
  while i < 5 do (
    sum := sum + x[i];
    i := i + 1
  );
  print_int(sum)
end
