let
  type iarr = array of int
  var arr := iarr [1] of 0
  var i := range (0,1)
  var j := range (0,1)
in
    if (i < j) then
      print_int(arr[i])
    else
      if (j < i) then print_int(arr[j])
end