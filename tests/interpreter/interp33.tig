let
  type int_arr = array of int
  var i := 0
  var idx := 0
  var arr := int_arr[2] of 10
in
  arr[0]:=0;
  arr[1]:=1;
  while i <= 1000 do (
    i := i+1
  );
  print_int(i);
  print("\n");
  (if i = 1001 then
    idx:=0
  else
    idx:=1);
  print_int(arr[idx])
end
